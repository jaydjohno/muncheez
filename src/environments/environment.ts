// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = 
{
	production: false,
	wordpressUrl: 'https://www.andreous.co.uk/wp-json',
	woocommerceUrl: 'https://www.andreous.co.uk/wp-json/wc/v3',
	consumer_key: 'ck_151b6a9762d8087839d64356120fcc3e691bffdb',
	consumer_secret: 'cs_621e679fe18936271b72e6a67e37d89ff5573dc6',
	webhook_secret: 'loJyf.[~p[{7,we# 6:)x4f}c5[#,aRLF[G++wblm+0#l/C>T;'
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
